Create scripts/audit/test-collateral-docs.ts. Login as credit_officer. Collateral: 1) POST /collateral valid invoice expect 201, 2) GET /collateral?invoice_id=X expect 1 item, 3) PUT /collateral/:id new value expect 200, 4) DELETE /collateral/:id expect 200 soft delete, 5) GET /collateral?invoice_id=X expect 0, 6) DELETE collateral on funded invoice expect 400/409. Documents: 7) GET /documents/:seed_id/download expect 200 with content-type, 8) GET /documents/nonexistent/download expect 404. Run it. Fix failures. Re-run until all 8 pass.