Read scripts/audit/backend-routes.json and scripts/audit/frontend-calls.json. For every frontend API call, compare the TypeScript response type against the actual backend response shape. Fix: amount fields returned as strings from NUMERIC columns need parsing to numbers. Date fields must be handled consistently. Pagination wrapper differences. Update all frontend TypeScript types to match actual backend response shapes after case transformation. Verify: npx tsc --noEmit — zero errors. npm test — all pass.