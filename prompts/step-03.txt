Check if the backend uses snake_case field names and the frontend uses camelCase. If yes, create a utility in the frontend shared lib with deepCamelCase(obj) and deepSnakeCase(obj) functions that recursively convert object keys. Handle nested objects, arrays, null, undefined. Skip FormData and Date values. Wire deepCamelCase into the Axios response interceptor for ALL responses. Wire deepSnakeCase into the Axios request interceptor for request bodies (skip FormData). Verify: run npx tsc --noEmit in the frontend directory — zero errors.