Create scripts/audit/test-collections.ts. Login as credit_officer from seed data. Test: 1) GET /collections expect paginated list, 2) GET /collections/:seed_id expect detail with payments, 3) POST payment with mtn_momo expect updated balance, 4) verify total_collected increased, 5) POST payment bank_transfer verify balance updates, 6) POST escalate different collection expect level+1, 7) verify escalation_date set, 8) escalate resolved collection expect error. Run it. Print pass/fail per test. Fix failures. Re-run until all 8 pass.