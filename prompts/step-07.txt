Create scripts/audit/test-dashboard-invoices.ts. Login as admin and supplier from seed data. Dashboard tests: 1) GET /dashboard/summary expect metrics, 2) period=7d, 3) period=30d, 4) as supplier expect scoped data, 5) GET payment history expect paginated array. Invoice tests: 6) GET /invoices as supplier expect own invoices, 7) POST /invoices valid data expect 201, 8) GET /invoices/:created_id expect detail, 9) PUT draft expect 200, 10) POST submit expect status change. Run it. Print pass/fail per test. Fix failures. Re-run until all 10 pass.