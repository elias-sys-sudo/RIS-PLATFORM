Read scripts/audit/backend-routes.json and scripts/audit/frontend-calls.json. For every frontend call, compare against its backend match. Find and fix: URL path differences, missing required request body fields, enum value mismatches (e.g. underReview vs under_review, MTN_MOMO vs mtn_momo). Fix every mismatch on the frontend side only. Do NOT change backend code. Print a table: frontend hook, issue found, fix applied. Verify: npx tsc --noEmit in frontend — zero errors.