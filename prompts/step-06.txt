Create scripts/audit/test-auth.ts that tests against the running backend. Test: 1) POST /auth/login with valid seed admin credentials expect 200+JWT, 2) login valid supplier expect 200+JWT, 3) login wrong password expect 401, 4) protected endpoint without JWT expect 401, 5) admin endpoint with supplier JWT expect 403, 6) POST forgot-password expect 200, 7) PUT change-password correct current expect 200, 8) change-password wrong current expect 400/401. Run it. Print pass/fail per test. Fix any failures. Re-run until all 8 pass.